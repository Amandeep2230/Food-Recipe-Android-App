<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.recipehub">

    <uses-permission android:name="android.permission.INTERNET" />

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".svid"></activity>
        <activity android:name=".mvid" />
        <activity android:name=".fvid" />
        <activity android:name=".dvid" />
        <activity android:name=".cvid" />
        <activity android:name=".spring" />
        <activity android:name=".manch" />
        <activity android:name=".fried" />
        <activity android:name=".dim" />
        <activity
            android:name=".chilli"
            android:label="@string/title_activity_chilli"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".main2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>